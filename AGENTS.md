# AGENTS.md

四川统招专升本三科（计算机 / 英语 / 高数）题库与错题本仓库的动态指令文件。给任何编码 Agent 读，先读本文件再动手。

## 一、仓库是什么

- 以 **Markdown 为唯一数据源** 驱动三科刷题：`题库.md`、`资料库.md`、`错题集/错题本.md`。
- **生成的 HTML（`题库页.html`、`复习页.html` 等）是构建产物，不要手工编辑、不要手工修复**——改数据源后再重新生成或由我按格式更新。
- 目录：
  ```
  计算机/   题库.md 资料库.md 题库页.html  错题集/(错题本.md 复习页.html)
  英语/     题库.md 资料库.md 题库页.html  错题集/(错题本.md 复习页.html 单词卡.md 单词卡.html 生词本.md 生词本.html 单词本.md 单词本.html)
  高数/     题库.md 资料库.md 题库页.html  错题集/(错题本.md 复习页.html)
  真题收集/    （历年真题资料）
  *.mjs / *.py  一次性生成/清理脚本（build_plan.mjs、rebuild_daily.mjs 等）
  ```

## 二、铁律（必须遵守）

1. **真题例题不许乱编。** 当被要求"出题/出例题/给真题"时，必须先 `WebSearch` 搜索**全国历年专升本真题**，找出真实题目来当，并在正文里**写明出自哪个省、哪一年**。找不到对应真题时，也要出一道与专升本难度相当的题——**绝不出一看就简单得离谱的凑数题**。
2. **数据源是 MD，产物是 HTML。** 只改 `.md`；`.html` 由生成脚本产出，不改数据源就去改 HTML 等于白改。
3. **错题本是个人刷题数据，永不提交。** `计算机|高数|英语/错题集/错题本.md` 已在 `.gitignore` 里，任何 git 操作不得强制加入。
4. **改动后要在 `progress.md` 记录证据**（改了哪个文件、跑了什么验证、下一步）。
5. **完成任务前必须验证**：至少一次语法/一致性检查，禁止未验证就声称"完成"。

## 三、题目 / 错题格式（沿用现库）

- 题库：`## 专题 / 章节` 分节；每题为 `**真题-科目-题型-序号.** 题干` → `- A. B. C. D.`（可选）→ `**答案：X**　解析：…` → `- 来源：…（尽量精确到省/年）`。数学公式用 LaTeX：行内 `$...$`、块级 `$$...$$`。
- 错题：`## Q-序号`，字段 `章节/题目/答案/解析/选项/题型/知识点/错因/错误次数/日期/状态`。英语生词：`## W-序号`，字段 `单词/音标/词性/释义/例句/词根/状态`。见各 `错题集/` 下 README。

## 四、验证命令（对应 init.sh）

```bash
bash init.sh            # 全量自检
node --check <file.mjs> # 单个脚本语法检查
git status              # 确认没有把 错题本.md 之类偷偷 add
```

Windows PowerShell 等价快速检查：
```powershell
Get-ChildItem *.mjs | ForEach-Object { node --check $_.Name }
git ls-files | Select-String "错题本.md"; git status --short
```

## 五、完成定义

- 任务目标实际达成（改了正确文件、语法正确）。
- 若涉及例题/真题，满足铁律 1（可追溯到省/年）。
- `progress.md` 已更新到能看懂的"下一步"。
- 无未声明的越界改动（不该动的 `.html` / `node_modules` / gitignore 文件）。

## 六、范围边界

- 本仓库只管**三科题库 + 错题本**。不要顺手重构无关脚本、不要批量改动未经要求的文件。
- 一次性 `.mjs`/`.py` 脚本多为清理/生成用，确认其目标后再跑，别盲目执行。